name: "dropbox-service"
replicaCount: 1

images:
  PullSecrets: 
    name: test-drive-secret
  repository: drivehub.azurecr.io/dropbox-service
  tag: "develop"

service:
  type: ClusterIP
  ports:
  - name: dropbox-service
    port: 8080

config:
  configmaps: [global-config]

ingress:
  enabled: false
