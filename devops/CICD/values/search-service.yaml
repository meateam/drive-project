name: "search-service"
replicaCount: 1

images:
  PullSecrets: 
    name: test-drive-secret
  repository: drivehub.azurecr.io/search-service
  tag: "develop"

service:
  type: ClusterIP
  ports:
  - name: search-service
    port: 8080

config:
  configmaps: [global-config]

ingress:
  enabled: false
